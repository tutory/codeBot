{
  "name": "tutory-codebot",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous GitHub issue worker scaffold",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/cli.js run-once",
    "test": "node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "@openai/codex": "^0.36.0",
    "@types/node": "24.12.0",
    "typescript": "^5.8.2"
  }
}
